<h1>Here are the users:</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, user_path(user) %>
      <%= button_to "Delete", user_path(user), method: :delete, data: { confirm: "Are you sure you want to delete this user?" } %>
    </li>
  <% end %>
</ul>
